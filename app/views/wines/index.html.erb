<h1>Wines</h1>
<p>Thanks for the second opportunity Andrew! <br /> It's still not complete - but much further along than perviously.</p>
<ol>
<% @wines.each do |wine| %>
  <li>
   <%= link_to(wine.name, wine) %>
   <br>
   <%= wine.year %> <%= wine.varietal %> bottled by <%= wine.winery %>, <%= wine.country %>
   <br>
  </li>
<% end %>
</ol>

<%= link_to "New Wine", new_wine_path, class: 'btn btn-primary' %>
